// Tailwind v4: configuration moved to CSS (@theme in globals.css).
// Safelist classes are declared via @source inline() in globals.css.
// This file is kept as an empty stub to avoid breaking any tooling that
// expects it to exist.
export default {};
